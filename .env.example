# Turkenya Tours & Safaris — environment variables
# Copy this file to `.env` on the server and fill in real values.
# NEVER commit the real .env (it is git-ignored).

# --- Database (PostgreSQL on Linode) ---
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://turkenya:CHANGE_ME@localhost:5432/turkenya?schema=public"

# --- Admin dashboard ---
# Shared secret the /admin page sends as the x-admin-token header.
# Use a long random string. Generate: openssl rand -hex 32
ADMIN_TOKEN="CHANGE_ME_long_random_string"

# --- AI concierge ---
# The chat auto-detects the provider. Use FREE Groq while starting out;
# switch to Claude later by setting CHAT_PROVIDER=claude.
#
# Groq (FREE, no card — get a key at https://console.groq.com):
GROQ_API_KEY=""
GROQ_MODEL="openai/gpt-oss-120b"
#
# Claude (Anthropic — paid, get a key at https://console.anthropic.com):
ANTHROPIC_API_KEY=""
CLAUDE_MODEL="claude-3-5-haiku-latest"
#
# Force a provider: "groq" or "claude". Leave blank to auto-detect
# (prefers Groq if its key is set).
CHAT_PROVIDER=""

# --- Lead notifications: WhatsApp to sales team (optional) ---
# Leads always save; this just pings the team in real time.
#
# Option A — CallMeBot (FREE, best while starting):
#   1. Save +34 644 51 95 23 to your phone contacts
#   2. WhatsApp it: "I allow callmebot to send me messages"
#   3. It replies with your API key
#   Then set the sales number (no +, e.g. 254722666644) and the key:
WHATSAPP_PHONE=""
WHATSAPP_APIKEY=""
#
# Option B — Generic webhook (WhatsApp Business API / Twilio / n8n relay).
# Receives POST { text, lead }. Leave blank if using CallMeBot.
WHATSAPP_NOTIFY_URL=""
